# name: test/sql/duckdb_graphar.test
# description: test duckdb_graphar extension
# group: [duckdb_graphar]

# Before we load the extension, this will fail
statement error
SELECT duckdb_graphar('Sam');
----
Catalog Error: Scalar Function with name duckdb_graphar does not exist!

# Require statement will ensure this test is run with this extension loaded
require duckdb_graphar

# Confirm the extension works
query I
SELECT duckdb_graphar('Sam');
----
GraphArDuck Sam 🐥

