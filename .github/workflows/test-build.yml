name: Temp Pipeline for loadable

on:
  push:
    branches: [ main, by_template_static ]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  pr-title-check:
    name: PR Title Check
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-22.04

    steps:
      - name: Check PR Title
        uses: actions/github-script@v6
        with:
          script: |
            const title = context.payload.pull_request.title;
            const pattern = /(^[mM]erge\s.*$)|(^[rR]evert\s.*$)|(^BREAKING CHANGE:.*$)|(^MINOR\s.*$)|(^(\w+)(?:\(([\w\$\.\-\*\s]+)\))?\:\s(.+)$)/;
            if (!pattern.test(title)) {
              core.setFailed(`PR title "${title}" doesn't match required format. Please use one of:
              - Conventional commit format: "type(scope): description"
              - Merge format: "Merge ..."
              - Revert format: "Revert ..."
              - Breaking change: "BREAKING CHANGE: description"
              - Minor change: "MINOR description"`);
            }

  lint:
    name: C++ Style Check
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Install clang-format-19
        run: |
          wget https://apt.llvm.org/llvm.sh
          chmod +x llvm.sh
          sudo ./llvm.sh 19 all
          sudo apt-get install -y clang-format-19

      - name: Verify C++ Code Style
        run: |
          find src/ include/ -name '*.cpp' -o -name '*.hpp' | xargs clang-format-19 --dry-run -Werror

  build:
    name: ${{ matrix.os }} Build and Test
    runs-on: ${{ matrix.os }}
    timeout-minutes: 120
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-22.04, macos-14]

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          submodules: true

      - name: Install System Dependencies (Ubuntu)
        if: matrix.os == 'ubuntu-22.04'
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends \
            build-essential cmake curl libcurl4-openssl-dev zlib1g-dev \
            libzstd-dev libsnappy-dev ccache python3-pip

      - name: Install System Dependencies (macOS)
        if: matrix.os == 'macos-14'
        run: brew install ccache

      - name: Set up specific CMake version (macOS)
        if: matrix.os == 'macos-14'
        uses: jwlawson/actions-setup-cmake@v2
        with:
          cmake-version: '3.28.3'

      - name: Set up ccache
        run: |
          mkdir -p ${{ runner.workspace }}/.ccache
          echo "CCACHE_DIR=${{ runner.workspace }}/.ccache" >> $GITHUB_ENV
          if [ "${{ matrix.os }}" = "ubuntu-22.04" ]; then
            echo "/usr/lib/ccache" >> $GITHUB_PATH
          elif [ "${{ matrix.os }}" = "macos-14" ]; then
            echo "/usr/local/opt/ccache/libexec" >> $GITHUB_PATH
          fi

      - name: Cache ccache
        uses: actions/cache@v4
        with:
          path: ${{ runner.workspace }}/.ccache
          key: ${{ runner.os }}-ccache-${{ hashFiles('Makefile', 'CMakeLists.txt', 'extension_config.cmake') }}
          restore-keys: |
            ${{ runner.os }}-ccache

      - name: Cache Arrow and GraphAr
        id: cache-third-party
        uses: actions/cache@v4
        with:
          path: |
            third_party/
          key: ${{ runner.os }}-third-party-${{ hashFiles('Makefile') }}
          restore-keys: |
            ${{ runner.os }}-third-party

      - name: Skip Arrow/GraphAr build if cached
        if: steps.cache-third-party.outputs.cache-hit == 'true'
        run: |
          echo "Arrow and GraphAr restored from cache"

      - name: Set up DuckDB
        id: duckdb-version
        run: |
          git submodule update --init --remote duckdb
          cd duckdb
          git fetch --tags origin
          cd ..
          DUCKDB_GIT_VERSION=$(cat .github/duckdb_version) make set_duckdb_version
          cd duckdb && echo "duckdb_hash=$(git rev-parse HEAD)"

      - name: Build
        run: |
          set -e
          for i in {1..2}; do
            if make release; then
              echo "Build succeeded on attempt $i"
              break
            else
              echo "Build failed on attempt $i"
              if [ $i -eq 2 ]; then
                echo "Build failed after 2 attempts. Exiting."
                exit 1
              fi
              echo "Retrying build..."
            fi
          done

      - name: Prepare Artifacts for Upload
        run: |
          mkdir -p upload-artifacts

          cp build/release/duckdb upload-artifacts/
          cp build/release/extension/duckdb_graphar/duckdb_graphar.duckdb_extension upload-artifacts/

      - name: Upload Build Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: duckdb-graphar-${{ matrix.os }}
          path: upload-artifacts
          if-no-files-found: error

